# Database Connection (Drizzle ORM)
# Replace with your actual PostgreSQL connection string
DATABASE_URL="postgres://user:password@host:port/db-name"

# Supabase Configuration
# Get these from your Supabase project settings
# URL: https://app.supabase.com/project/_/settings/api
# Note: VITE_ prefix is required for Vite to expose these as public variables
VITE_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
VITE_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# IMPORTANT: Never commit the service role key to version control
# Only use this on the server side for admin operations
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Session Configuration
# SESSION_SECRET="your-random-secret-key-min-32-chars"
